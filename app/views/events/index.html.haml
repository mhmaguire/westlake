-provide :title, "| #{get_event_title}"
#eventsPage.stackPage
	#eventSlider
		=render @events



=content_for :scripts do
	:javascript
		$(function() {
			$('#eventSlider').orbit({
				animationSpeed: 700,
				advanceSpeed: 10000,
				directionalNav: true,
				bulletThumbs: true
			});
		});
