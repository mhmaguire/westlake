%li.category
	.title
		%h3
			%a{name: "##{category.param_title}"}= category.title
	.conent
		%ul.vendors.block-grid.three-up
			=render partial: 'shared/vendor', collection: category.vendors.order(:name) 