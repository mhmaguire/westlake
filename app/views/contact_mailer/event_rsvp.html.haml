%h1= "New event RSVP for #{@event_contact.event.title}"
%p= "from #{@event_contact.name} #{@event_contact.last_name}"
%p= "Email #{@event_contact.email}"
%p= "Phone #{@event_contact.phone}"
%p= "Source #{@event_contact.source}"