.staticPage
	.staticContent
		.announcementShow
			%h2= @announcement.title
			%ul.block-grid.three-up
				-@announcement.vendors.each do |v|
					%li
						=link_to v.href do
							=image_tag v.image.url(:medium), retina: true
			=raw @announcement.body